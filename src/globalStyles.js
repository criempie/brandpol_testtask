import { StyleSheet } from "react-native";

export const globalStyles = StyleSheet.create({
    icon: {
        width: 32,
        height: 32,
    },

    h1: {
        fontSize: 24,
    },

    h2: {
        fontSize: 20,
    },

    h3: {
        fontSize: 16,
    }
});
